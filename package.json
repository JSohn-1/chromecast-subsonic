{
  "name": "chromecastaudio",
  "version": "1.0.0",
  "description": "plays subsonic streams to chromecast with a restful api",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "run": "node ./dist/src/index.js",
    "bun-run": "bun ./src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "windowsDebug": "@powershell -Command $env:DEBUG='chromecast-api';node dist/src/index.js > log.txt 2>&1"
  },
  "author": "yacob",
  "license": "ISC",
  "devDependencies": {
    "@types/bun": "^1.0.2",
    "bun-types": "latest"
  },
  "dependencies": {
    "@spotify/web-api-ts-sdk": "^1.2.0",
    "@types/chromecast-api": "^0.4.5",
    "chromecast-api": "^0.4.2",
    "crypto-random-string": "^3.3.1",
    "express": "^4.18.2",
    "md5": "^2.3.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
